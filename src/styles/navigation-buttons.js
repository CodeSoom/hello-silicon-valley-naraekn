import styled from '@emotion/styled';

const ButtonBarLayout = styled.div({
  zIndex: 10000,
  position: 'fixed',
  bottom: 0,
  width: '100%',
  maxWidth: '500px',
});

export default ButtonBarLayout;
